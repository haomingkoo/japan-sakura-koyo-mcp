{
  "type": "object",
  "properties": {
    "dateStyle": {
      "type": "string",
      "title": "Date Style",
      "description": "How dates should be rendered in tool output.",
      "x-from": { "query": "dateStyle" },
      "enum": ["friendly", "iso"],
      "default": "friendly"
    },
    "temperatureUnit": {
      "type": "string",
      "title": "Temperature Unit",
      "description": "Preferred temperature unit for weather output.",
      "x-from": { "query": "temperatureUnit" },
      "enum": ["celsius", "fahrenheit"],
      "default": "celsius"
    },
    "includeCoordinates": {
      "type": "boolean",
      "title": "Include Coordinates",
      "description": "Whether GPS coordinates should be included for spots, farms, and festivals.",
      "x-from": { "query": "includeCoordinates" },
      "default": true
    },
    "mapLanguage": {
      "type": "string",
      "title": "Map Language",
      "description": "Preferred language for forecast map links when both English and Japanese URLs are available.",
      "x-from": { "query": "mapLanguage" },
      "enum": ["english", "japanese"],
      "default": "english"
    }
  }
}
